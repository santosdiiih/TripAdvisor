<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="colorPrimaryDark">@color/blue_ligth</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="StyledTilEditTextTheme">
        <item name="colorControlNormal">@color/black</item> // Cor padrão da linha
        <item name="colorControlActivated">@color/black</item> // Cor linha e texto quando recebe foco
    </style>

    <style name="StyledTilEditText">
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:textColorHint">@color/black</item> // Cor texto(hint) sem foco
    </style>

</resources>
